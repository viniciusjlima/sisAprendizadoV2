@model Aprendizado.Entity.Pergunta_Atividade

@{
    ViewBag.Title = "EditPerguntaAtividade";
}

<h2>Escolher pergunta</h2>

@using (Html.BeginForm())
{
    <div class="form-horizontal">
        <h4>Incluir Pergunta</h4>
        <hr />

        @Html.HiddenFor(model => model.idPerguntaAtividade)

        @*@Html.HiddenFor(model => model.idPergunta)*@

        @Html.HiddenFor(model => model.idAtividade)

        <div id="divPergunta">
            <div class="form-group">
	            @Html.LabelFor(model => model.Pergunta, new { @class = "control-label col-md-2" })
	            <div class="col-md-10">
		            @Html.DropDownList("idPergunta", "Seleciona uma pergunta")
	            </div>
            </div>
        </div>

        <div>
            <br />   
                <input type="button"  class="btn btn-default" value="Voltar" onClick="    history.go(-1)">  <input type="submit" value="Salvar"  class="btn btn-default" onclick="    return validar();"/> 
        </div>
    </div>
}



@if (ViewBag.Erro != null && ViewBag.Erro != "")
{
    <script>
        alert('@Html.Raw(ViewBag.Erro)');
    </script>
}

<script>
    function validar() {


        return true;
    }

</script>
